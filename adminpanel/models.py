# This app manages existing models from learning, tests_app, practice and
# django.contrib.auth. It intentionally defines no models of its own, except
# for PracticeSettings which lives in practice/models.py (see the patched
# version of that file) since it configures practice/recognition behaviour.
